# agent/

The `doorstep_agent` package: coordinator Graph, agents, tools, hooks, policies and memory,
built with the Strands Agents SDK. Hazard profiles live in `doorstep_agent/profiles/` (SPEC §3a).
Cedar policies live in `policies/`. Arrives in Phase 1.
