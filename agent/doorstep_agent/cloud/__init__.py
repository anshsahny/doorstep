"""Doorstep in AWS: the coordinator on Amazon Bedrock AgentCore Runtime (PLAN Phase 3).

Built with the Strands Agents SDK. Everything here is wiring — how events arrive, where records
and sessions are kept, where secrets come from. The judgement stays in the modules the local
drill already uses.
"""
