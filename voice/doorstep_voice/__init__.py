"""Doorstep voice: the SPEC §9 check-in spoken by a Strands Agents BidiAgent on Nova 2 Sonic.

One call session (`session.VoiceCheckin`) and two ways in: the browser, over the AgentCore
Runtime WebSocket (`entrypoint`), and a phone call, over Twilio Media Streams (`twilio`).
"""
