# web/

The dashboard: React 19 + Vite + TypeScript + Tailwind 4, no map library (the door grid lays out
the building's floors and streets). Pages: board, decisions, policies, report, evidence, captain.
`make web` (dev server against the deployed API), `make web-test`, `make web-deploy`. Node 22.
